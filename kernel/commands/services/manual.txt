Manage services in system

Required permissions:
    root

```
Actions available:
    list       : List current available services
    configure  : Configure a service
    loaded     : Show loaded services in system with stages
```

```
Usage:
    services list
    services configure <service_name> <parameters...>
```