Change tags (permissions) of a user

Required permissions:
    admin
    
```
Actions available:
    add     : Add a tag to a user
    list    : List all tags of a user
    remove  : Remove a tag from a user
```

```
Usage:
    tags <action> <@user mention> <tag name> <tag value>
```